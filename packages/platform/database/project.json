{
  "name": "@iam/database",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/platform/database/src",
  "projectType": "library",
  "tags": ["type:platform", "scope:database"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/platform/database"
      }
    }
  }
}
